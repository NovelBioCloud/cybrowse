import {
	StyleProvider,
	MappingStyleProvider
} from '../../core/style';
import {
	edgeStyle
} from '../../etc';
const edgeStyleProvider = new StyleProvider("edge", edgeStyle);

export default edgeStyleProvider;
